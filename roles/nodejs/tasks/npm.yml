---
- name: asdf local
  command: asdf local nodejs {{ version }}

- name: npm update -g
  command: npm update -g
  ignore_errors: true

- name: npm install -g
  command: npm install -g {{ item }}
  # community.general.npm:
  #   global: yes
  #   name: "{{ item }}"
  #   state: latest
  loop:
    - "@anthropic-ai/claude-code"
    - "@devcontainers/cli"
    - "@github/copilot"
    - "@github/copilot-language-server"
    - "@github/local-action"
    - "@google/clasp"
    - "@google/gemini-cli"
    - "@hubspot/cli"
    - "@marp-team/marp-cli"
    - "@openai/codex"
    - chrome-devtools-mcp
    - clear-npx-cache
    - corepack # https://yarnpkg.com/getting-started/install
    - nbb
    - npm-check-updates
    - npm-safety-updater
    - prettier
    - sql-formatter
    - textlint
    - typescript
    - typescript-language-server
    - yarn
    - yarn-deduplicate

- name: asdf reshim
  command: asdf reshim nodejs
